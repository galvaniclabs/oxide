//! Oxide editor core: buffer, cursor, selection, undo/redo.
//!
//! This crate contains the core editing primitives used by Oxide IDE.
//! It knows nothing about rendering — that's `oxide-tui`'s job.
